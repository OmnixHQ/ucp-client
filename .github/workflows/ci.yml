name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Build, Lint & Test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Build
        run: npm run build

      - name: Validate exports
        run: npm run check:exports

      - name: Validate package
        run: npm run check:publish

      - name: Run tests with coverage
        run: npm run test:coverage

      - name: Upload coverage to Codecov
        if: always()
        uses: codecov/codecov-action@v5
        with:
          files: coverage/lcov.info
          fail_ci_if_error: false

  integration:
    name: Integration (mock server)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: ci

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Start mock UCP server
        run: PORT=3002 npx tsx scripts/mock-ucp-server.ts &

      - name: Wait for server to be ready
        run: |
          for i in $(seq 1 30); do
            curl -sf http://localhost:3002/.well-known/ucp > /dev/null && echo "Server ready" && exit 0
            sleep 1
          done
          echo "Server did not start in time" && exit 1

      - name: Run integration tests
        run: INTEGRATION=true GATEWAY_URL=http://localhost:3002 npx vitest run tests/integration.test.ts tests/adapters-catcherrors.test.ts

  agent-openai:
    name: Agent — OpenAI adapter
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: ci

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Start mock UCP server
        run: PORT=3002 npx tsx scripts/mock-ucp-server.ts &

      - name: Wait for server to be ready
        run: |
          for i in $(seq 1 30); do
            curl -sf http://localhost:3002/.well-known/ucp > /dev/null && echo "Server ready" && exit 0
            sleep 1
          done
          echo "Server did not start in time" && exit 1

      - name: Run OpenAI agent tests
        run: INTEGRATION=true GATEWAY_URL=http://localhost:3002 npx vitest run tests/agents/openai-agent.test.ts

  agent-anthropic:
    name: Agent — Anthropic adapter
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: ci

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Start mock UCP server
        run: PORT=3002 npx tsx scripts/mock-ucp-server.ts &

      - name: Wait for server to be ready
        run: |
          for i in $(seq 1 30); do
            curl -sf http://localhost:3002/.well-known/ucp > /dev/null && echo "Server ready" && exit 0
            sleep 1
          done
          echo "Server did not start in time" && exit 1

      - name: Run Anthropic agent tests
        run: INTEGRATION=true GATEWAY_URL=http://localhost:3002 npx vitest run tests/agents/anthropic-agent.test.ts

  agent-mcp:
    name: Agent — MCP adapter
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: ci

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Start mock UCP server
        run: PORT=3002 npx tsx scripts/mock-ucp-server.ts &

      - name: Wait for server to be ready
        run: |
          for i in $(seq 1 30); do
            curl -sf http://localhost:3002/.well-known/ucp > /dev/null && echo "Server ready" && exit 0
            sleep 1
          done
          echo "Server did not start in time" && exit 1

      - name: Run MCP agent tests
        run: INTEGRATION=true GATEWAY_URL=http://localhost:3002 npx vitest run tests/agents/mcp-agent.test.ts

  agent-vercel-ai:
    name: Agent — Vercel AI adapter
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: ci

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Start mock UCP server
        run: PORT=3002 npx tsx scripts/mock-ucp-server.ts &

      - name: Wait for server to be ready
        run: |
          for i in $(seq 1 30); do
            curl -sf http://localhost:3002/.well-known/ucp > /dev/null && echo "Server ready" && exit 0
            sleep 1
          done
          echo "Server did not start in time" && exit 1

      - name: Run Vercel AI agent tests
        run: INTEGRATION=true GATEWAY_URL=http://localhost:3002 npx vitest run tests/agents/vercel-ai-agent.test.ts

  agent-langchain:
    name: Agent — LangChain adapter
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: ci

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Start mock UCP server
        run: PORT=3002 npx tsx scripts/mock-ucp-server.ts &

      - name: Wait for server to be ready
        run: |
          for i in $(seq 1 30); do
            curl -sf http://localhost:3002/.well-known/ucp > /dev/null && echo "Server ready" && exit 0
            sleep 1
          done
          echo "Server did not start in time" && exit 1

      - name: Run LangChain agent tests
        run: INTEGRATION=true GATEWAY_URL=http://localhost:3002 npx vitest run tests/agents/langchain-agent.test.ts
